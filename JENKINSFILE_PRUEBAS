pipeline {
    agent { label 'agente-deploy'}
    
    stages {
        stage('Verificar agente-clonador') {
            steps {
                sh 'echo "Hola desde agente- clonador"'
                sh 'whoami'
                sh 'hostname'
            }
        }
    }
}